# Engineering Notes (project brain)

Dense session-to-session engineering knowledge for SimBuilder and its
sibling repos (mf4-viewer, plt-to-mf4-converter, csv-to-mf4-converter).
Written by/for Claude Code sessions but useful to any human continuing the
work on any machine. Newest entries first. Paths refer to George's primary
machine; the deck patcher heals machine-specific paths automatically.

Related: README.md (architecture + build commands), CAMPAIGN.md (the
simulation to-do list).

---

Unified app (built 2026-07-16) at `C:\Users\George\OneDrive\Desktop\PhD Thesis\pipeline-app`: pywebview hosts the SimBuilder HTML with a RUN IN MOTIONSOLVE button (feature-detected via `window.pywebview`; hidden in plain browser). See the csv-to-mf4-converter repo.

**FMU MOTOR-DATA INJECTION BUILT 2026-07-17** (`fmu_inject.py`, George's ask): makes uploaded motor maps / motor fields reach decks whose motor data is INSIDE the FMU (LYRIQ). `inject_motor_fmu(deck_text, run_dir, spec, log)`: scans deck FMU refs (already abs after patch_deck), opens each as a zip, and the one containing `resources/*_motor_data.mat` is the motor FMU (EPAS's `epas_control_with_motor.fmu` also has 'motor' in the name, so match by CONTENTS not filename). Copies that FMU into run_dir, rewrites its internal frnt/rear `_motor_data.mat` + `_motor_char.mat` from the spec, re-points the deck string at the run copy. Per motor: if `effMapPath` is a FULL motor_data .mat (has m_spd_data/m_max_trq/m_map_eff_spd/m_map_eff_trq/m_eff_map) it's regridded WHOLE (envelope+eff+regen) onto the FMU's fixed 15x14 (`_resample_full`); else falls back to `motor_gen.build_motor_data` (fields+eff). motor_char rebuilt from the data (`_char_from_data`: trq_rated=max envelope, spd_max=max speed, corner speed at 99% peak). Zip rewrite preserves every non-.mat entry byte-for-byte (48/48; modelDescription + win64 dlls intact). Wired in `apply_vehicle`: only when `generate_motors` AND `motor_gen.generate_motor_files` wrote NOTHING (i.e. no external motor .mat — cleanly separates LYRIQ from the doublelane deck, which keeps using external files). Spec hidden-import `fmu_inject` added. VERIFIED end-to-end (lab venv, no solver): prepare_run on the local LYRIQ deck + George's uploaded `_frnt_motor_data.mat`(49x50)/`_rear_motor_data.mat`(14x21) → run-folder FMU with front max_trq 235.2 / rear 197.3 (=uploaded envelopes), 15x14 grids, deck re-pointed, well-formed XML, EPAS/ESP untouched. **NOT SOLVER-VALIDATED YET** — needs one MotionSolve run to confirm the FMU accepts the rewritten resources (grid shape preserved so it should; the FMU reads its axes from the .mat). effMapPath is being overloaded as 'full motor-data source' — a dedicated 'motor data file' field/label would be clearer later. Envelope note: when a full file is supplied, the motor's peak power/torque/rpm FIELDS are overridden by the file's envelope (fields become decorative for that motor).
**LYRIQ GEARING PATCH FIXED + MASS DEFAULT OFF + MOTOR-DATA IS IN-FMU 2026-07-17**:
(1) GEARING NOW TAKES: `patch_gear_ratios` expression leg was matching the COUPLER's old value (placeholder 3.7) inside the gearbox expressions — but this deck's expressions already carry the REAL per-axle ratios (front marker 336001 = 18, rear marker 340001 = 9.59), so 3.7 was never found → "ratio literal 3.7 not found" ×4, motor kept old gearing. FIX: patch the literal BY POSITION — the number in `*<ratio>*AKISPL(VARVAL(33200300),0,<marker>)` — via `re.subn(r'(\*\s*)[0-9.]+(\s*\*\s*AKISPL)', ...)`, independent of the coupler value. Verified on the real LYRIQ deck: Motor1=9.59/Motor2=18 → couplers 364002→9.59, 365002→18 AND all 4 expressions (336001→9.59, 340001→18). ⚠ DECK IS BUILT REVERSED vs George's stated physical layout: deck ships front(336001/364002)=18, rear(340001/365002)=9.59; George says physical front=9.59, rear=18. Vehicle Builder maps Motor1→deck-front(336001/364002), Motor2→deck-rear(340001/365002). ASKED George which assignment is physically correct before campaign (getting it backwards silently swaps his gearing). Couplers were BOTH placeholder 3.7 (inconsistent with expressions — deck as exported).
(2) MASS DEFAULT → OFF: George doesn't want spec-sheet mass auto-applied. `web/vehicle.js` default `applyMass:false` (line ~32) + undefined-migration →false (line ~67); current stored vehicle (SN-4097750050) forced applyMass=false via localStorage+clean reload (verified: stored/live/checkbox/payload all false). `patch_mass` still available via the toggle.
(3) MOTOR .mat FILES CAN'T ATTACH ON LYRIQ (as-is): this deck has ZERO external `.mat` refs — motor data lives INSIDE `Motor_PMSM_dual.fmu` at `resources/one_strlineacc_0_{frnt,rear}_motor_data.mat` (6248 B each = 15×14 grid) + `_motor_char.mat` + `_opt_trq_ratio.mat`. So `motor_gen` finds no `*frnt*/*rear*/*char*` files in the run folder → generates nothing (why the 14:58 run had no .mat), and `mat_overrides` has nothing to shadow. George's uploaded files (`C:\Users\gfare\OneDrive\Desktop\MBD - Copy For Testing\_frnt_motor_data.mat` 49×50, `_rear_motor_data.mat` 14×21) are VALID motor_data schema but non-standard/ mismatched grids. To make them take, need a NEW feature: copy the FMU into the run dir, replace its internal motor_data.mat (re-gridded to 15×14) + re-point the deck's FMU string ref. ASKED George whether to build it. NOTE FMU lives in Program Files\Altair\2025.1 (shared install — never edit in place; must copy-per-run).
**MALFORMED-XML DECK CRASH FIXED 2026-07-17 (lab, LYRIQ campaign)**: LYRIQ runs died in ~2 s with `ERROR: not well-formed (invalid token) at line 21198` BEFORE integrating — a bare `&` in the tire property-file `string="..."`, from the share folder name `Vehicle Dynamics & Calibration`. `&` is reserved in XML; the solver's strict parser (expat-class) rejects the whole deck. Two ways it got in: (a) the source FAKE_LYRIQ deck referenced the share path directly; (b) even after George re-copied the model LOCAL (clean relative `../../../Vehicle Data/LYRIQ_tire.tir`), the Vehicle Builder's stored tire override (`veh.tirePath` = the `\\Ecocar-r350\...\Vehicle Dynamics & Calibration\...` share path) got RE-INJECTED by `apply_vehicle`'s tire regex — which matches relative `../` refs too, not just drive-letter/UNC (I wrongly told George it wouldn't apply). FIX = `pipeline.xml_escape_amps(deck_text, log)` called in prepare_run AFTER patch_deck+apply_vehicle, before writing: escapes every `&` not already an entity via `&(?!(?:amp|lt|gt|quot|apos|#\d+|#x..);)` → `&amp;` (no double-escape; catches override-injected AND source-deck `&`, any machine/any deck). Verified: the real 14:58 failing run deck → WELL-FORMED after escape; full `prepare_run` dry-run (lab venv, no solver) on the local deck + current vehicle → emitted deck WELL-FORMED, tire line = local `C:/Users/gfare/.../MBD - Copy For Testing/Vehicle Data/LYRIQ_tire.tir`, 0 bare `&`, 0 share refs. NOTE George went local: settings deck = `C:\Users\gfare\OneDrive\Desktop\MBD - Copy For Testing\LYRIQ\HEAD\Trials 12\LYRIQ_-_HEAD_-_Trials_4_Run_driverfile_0.xml` (relative refs resolve to the local tree; validator: 11/11 refs local, well-formed). Current stored vehicle (SN-4097750050, "My EV", 92/65 kW, mass 2230) already has tirePath="" + effMapPath="" — no share paths; the share-path vehicle (SN-3630224743) was overwritten in localStorage.
**"OPEN RUN FOLDER" ON FAILED RUNS FIXED 2026-07-17**: on any run that failed during setup/solve, the button did nothing because `Api.last_run_dir` is only set AFTER `run_scenario` returns (it raises first). Added `dir_holder` dict plumbed run_scenario→prepare_run, set to run_dir the moment `os.makedirs` succeeds; main.py `_worker`/`_batch_worker` reset it per run and, in `except`, set `self.last_run_dir = dir_holder["dir"] or last_run_dir` before `msPipe.done(false,...)`. Now a crashed run's folder (with its .log) opens from the button.
**LYRIQ GEAR-RATIO PATCH GAP (noted, not fixed) 2026-07-17**: on the LYRIQ deck, `patch_gear_ratios` logs `WARNING: ratio literal 3.7 not found in gearbox expression - not patched` ×4 — the power-path `Reference_Variable` expressions ('Torque from Gear Box'/'Gear Box Input Staft Speed') don't carry the literal in the form the doublelane-validated regex expects. Couplers may still patch, but the expression leg doesn't. Harmless when gearRatio == deck default (3.7), but a real gear-ratio override won't fully apply on LYRIQ until the LYRIQ gearbox-expression format is reverse-engineered like the doublelane one was. Revisit if George sweeps gearing on LYRIQ.
**FILE DIALOGS + CROSS-MACHINE SETTINGS FIXED 2026-07-17 (lab)**: (1) NO-POPUP ROOT CAUSE: pywebview 6.2.1 (installed into the shared venv TODAY ~10:44) STRICTLY validates file_types via parse_file_type regex — the WinForms-style `'Name (*.ext)|*.ext'` strings main.py used since day one now raise ValueError → the JS promise rejects → dialog silently never opens (pick_deck/pick_file/import_drive_pick/export CSV; FOLDER_DIALOG unaffected). Fixed: pipe-less `'Name (*.ext)'` literals + `file_filter()` helper in main.py that strips a `|` tail from any JS-supplied spec (vehicle.js still sends pipe-style — works via the helper). Valid on old and new pywebview. Verified end-to-end via CDP: pick_deck promise stays PENDING with the native dialog open (was instant reject). (2) SETTINGS NOW MACHINE-PORTABLE: `pipeline._heal_user_path` in load_settings — if a stored path is missing, retry `C:\Users\<other>\rest` under the current profile (in-memory only, symmetric, no write-back → OneDrive ping-pong harmless). Verified: George's dist\settings.json heals to gfare deck/runs_dir with deck_ok+motionsolve_ok true, zero manual repointing. EXE REBUILT (lab venv, spec).
**HI-DPI CANVAS RUNAWAY FIXED 2026-07-17 (lab machine, gfare)**: on displays with devicePixelRatio > 1 (lab 4K @150% → dpr 1.5) `drawPlot` re-read the canvas `height` ATTRIBUTE it had itself multiplied by dpr on the previous call → exponential growth every refresh: page miles long, preview canvases empty/stretched, renderer eventually so wedged that clicks (e.g. Change chassis template → pick_deck) died silently. Invisible at home (dpr=1: 200×1=200 forever). Fix in web/app.js drawPlot: capture the HTML height attribute ONCE into `canvas.dataset.cssH` before assigning canvas.height. Verified via CDP on the live exe (WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS=--remote-debugging-port=9222 + websocket-client, suppress_origin=True): heights 270/330 (=180/220×1.5) constant over 8 refreshes. EXE REBUILT from SimBuilder.spec (spec is more complete than the README command: has drive_import/results/ems_builder/motor_gen hidden-imports). LAB MACHINE FACTS: shared venv in csv-to-mf4-app\.venv is DEAD here (pyvenv.cfg home → C:\Users\George\...\Python312, machine has only 3.13) — lab build venv created at `C:\Users\gfare\venvs\simbuilder-lab` (py3.13: pyinstaller pywebview asammdf numpy scipy matplotlib psutil tkinterdnd2). OPEN ISSUE: dist\settings.json is OneDrive-synced but holds machine-absolute paths (currently George's) — the two machines will ping-pong it; consider per-machine settings (key by username/hostname) or healing `C:\Users\<other>\` → current profile in load_settings.
**DEPLOY + GUARDS + VISUALIZERS 2026-07-17** (commits: simbuilder 0928144, mf4-viewer 1320f70, plt a5d0b28; BOTH exes rebuilt): (1) `confirmTimestep(h)` popup on EVERY run entry (single/AVL cycle/UDDS/HWFET/import) — George's hard rule: 10ms only; non-10ms gets ⚠⚠ warning. (2) Travel map in import card: canvas polyline red(slow)→green(fast) hsl(135·f); Python returns ≤600-pt preview (path or straight distance-ribbon). (3) **VehPosX/Y channels** (m, ×0.001 from 'Driver inputs 1' request comps 'Veh CG-X/Y') in both avl_extract copies → 24 channels; viewer got **🗺 Track map** button (LineCollection, RdYlGn, panel per file, shared scale, equal aspect). (4) **DDF PATH-FOLLOWING VERDICT: BROKEN IN THIS DECK/DRIVER** — driver logs 'Instantiating path from DDF…read…instantiated' then steers ZERO and force-terminates (~23s, 'path error > 1 mm' warnings; ENDFLG). Proven NOT our ADF: stock doublelane ADF (steering works with PREDEFINED) surgically re-pointed at DDF → still dead straight. mm-vs-m DDF units made no difference (identical trajectories → driver converts units fine). Parked behind `experimental_ddf` checkbox (default off, honest warning); imports default to proven speed-following. Path recon still powers travel map. Possible future: Altair support ticket / PREDEFINED 'CURVE' path grammar hunt (RoadCourse's Curve path emission never located in decompiled templates).
**REAL-DRIVE IMPORT 2026-07-17** (`drive_import.py`, commit b9c6ac7): logged MF4 → runnable scenario. Speed-only mode → shared `drive_cycles.build_adf_from_series` (refactored out of build_cycle_adf). LATERAL: **DDF grammar from Altair's own Snet_path example** (`ADF_examples\Snet_path.adf/.ddf` in hwdesktop mdllib Tasks\AltairDriver): DDF = own [UNITS] (meters!) + `[DEMAND_VECTORS] {X Y Z DV}`; ADF: `[FEEDFORWARD_STEERING] PATH='DDF' FILE='x.ddf' AGGRESSIVE='TRUE'` + `[DEMAND_SPEED] TYPE='CURVE' FILE='x.ddf' DEMAND_VECTOR='DV'`. Path sources: yaw dead-reckon (∫r→ψ, ∫v cosψ — VALIDATED: synthetic 100m circle → 100.0m exact, path/odo 100.0%), steering bicycle model (uses veh steerRatio+wheelbaseMm), GPS equirectangular. ~2m path resample. `pipeline aux_files={fname:text}` plumbing writes DDF into run dir. Api: import_drive_pick/import_drive_build(cfg)/run_imported. UI card in Scenario tab w/ channel dropdowns + name-based guessing + path-vs-odometer ⚠ check. **NOT SOLVER-VALIDATED YET & EXE NOT REBUILT** — George had a 10% experiment running (rule: NEVER rebuild/kill SimBuilder while his runs live; solver tests also deferred to avoid CPU contention). TODO next session: rebuild exe (README command + cycles + results hidden-imports), one short solver validation of a DDF path scenario (does the deck's driver accept PATH='DDF'? — grammar is from official examples but UNPROVEN in this deck).
**DARK MODE + CAMPAIGN PLAN 2026-07-16 (night)**: header 🌙/☀️ toggle → `body.dark` (CSS var overrides + hardcoded-surface overrides; **canvases/SVG get light backing in dark mode** since JS draws dark text — don't "fix" chart colors in JS). Persisted `simbuilder.theme` localStorage. **`pipeline-app\CAMPAIGN.md` = George's agreed to-do list** (committed): Phase 0 calibration (lock vehicle+serial, 1 full HWFET deck-default overnight, 1×10% tip-in for cost sizing, morning RMSE check), Phase 1 HWFET×6 EMS sweep (**needs RUN EMS SWEEP button — item 3, NOT BUILT YET, he'll ask**), Phase 2 UDDS (maybe UDDS-Phase-1 505s variant), Phase 3 AVL CYCLE×3 + lab AVL calibration table, Phase 4 ±10% mass + real motor maps, Phase 5 export/write. Standing rules in file (don't change vehicle mid-phase, RMSE validity gate, determinism).
**RESULTS TAB 2026-07-16** (campaign item 2, DONE): 4th tab "Results". `results.py`: `scan_runs(runs_dir, force)` → rows cached in `<runs_dir>\.simbuilder_results_cache.json` keyed by MF4 mtime; `metrics_from_mf4`: duration, dist_km (trapezoid ∫v — **NumPy 2.x: np.trapz REMOVED, use np.trapezoid**), energy_kwh (∫BattPower net), wh_per_km (needs dist>0.05km), soc_drop_pct, v_max, jerk_rms (d(accel)/dt RMS), chatter_per_min (EM torque |T|>2Nm on/off transitions), track_rmse_kph (UDDS/HWFET name prefix → vs EPA trace, t>5s), mf4_serial + serial_ok cross-check vs vehicle.json. `web/results.js`: sortable table (click headers), per-row 📂/📈 (Api.open_path / view_mf4 → self --viewer), Wh/km bars + efficiency-vs-jerk scatter (canvas, EMS_COLORS palette per strategy), Export CSV (Api.export_results_csv, SAVE_DIALOG, utf-8-sig). switchTab list now 4 tabs; resultsOnEnter fetches on entry; browser shows hint. Build adds `--hidden-import results`. Old runs w/o BattPower/VehicleSerial show "—" gracefully. Campaign item 3 REMAINING: RUN EMS SWEEP button.
**DRIVE CYCLES + BattPower 2026-07-16** (start of the EMS-efficiency campaign, item 1): `drive_cycles.py` + `cycles/uddscol.txt,hwycol.txt` (EPA official, curl'd from epa.gov; 2 header lines then "sec\tmph"). ADF grammar for cycles CONFIRMED from ADFtemplates_DECOMPILED.py: `[DEMAND_SPEED] TYPE='CURVE' BLOCK='DEMAND_CURVE'` + `[DEMAND_CURVE] INDEPENDENT_VARIABLE='TIME' DEPENDENT_VARIABLE='VELOCITY' INTERPOLATION='AKIMA'` + table rows; **demand velocities in mm/s** (stock doublelane VALUE=10000=36kph). Driver falls back FF→feedback PID traction ("No solver info found for engine") and tracks anyway. **VALIDATED: 60s HWFET segment → 0.42 km/h RMSE tracking**; 887 wall-s for 60 sim-s (~15x, incl. standstill launch) → full HWFET ≈ 1.5-3.5h, UDDS (17 stops) overnight. UI: UDDS/HWFET buttons in preset card → `Api.run_cycle(name, vehicle)`. Build adds `--add-data "cycles;cycles" --hidden-import drive_cycles`. **BattPower channel** added to avl_extract (BOTH pipeline-app AND plt-to-mf4-converter repo, pushed a4bce30): R_BATTERY component 'Combined power demand' [W]→kW, +discharge/−regen; between BattPower/BattSOC in COLUMN_ORDER → 22 channels now. Efficiency metrics: Wh/km = ∫BattPower/∫v. NEXT (agreed plan): item 2 = Results tab (campaign leaderboard: Wh/km, ΔSOC, tracking RMSE, jerk RMS, chatter count, efficiency-vs-drivability scatter), item 3 = RUN EMS SWEEP button. Determinism: repeats pointless, sweep strategies/cycles instead. FIALA A/B verdict: George's fiala_LYRIQ file BROKEN in this deck (car never moved, chatter, 21x SLOWER) — MF-Swift standstill fine at 35% pedal (15x realtime); gentle pedals are the slow ones.
**RUN AVL CYCLE 2026-07-16** (George's batch ask, "I don't have forever"): green button in Scenario tab preset card → `Api.run_batch(runs, vehicle)` runs all 8 presets (PRESET_PCTS 10-70,100% pedal; `presetTip()` in web/app.js, hmax already 0.01=10ms — George said "0.1s" once but stressed 10ms; resolved to 10ms) SEQUENTIALLY: per-run folder+MF4, `viewer_launcher=False` suppresses per-run viewer ("batch mode" log), overall progress = (i+frac)/n, STOP aborts remainder, failures continue batch, `os.startfile(runs_dir)` at end. JS builds ADFs by temporarily swapping global `sc` through `presetTip(pct)` + `generateSegments()`. `openRunPanel()` shared helper preps panel for single+batch. **Shadow bug fixed**: main.py dev-mode sys.path must point at mf4-viewer-app NOT csv-to-mf4-app (its converter.py shadowed pipeline's converter — ImportError DEFAULT_PACK_VOLTAGE; frozen exe was immune). Presets are standstill starts — batch takes real hours; George accepts.
**RESTRUCTURED INTO 4 PUBLIC REPOS 2026-07-16** (all github.com/George-F-2000, created via API + stored git credential, no Claude attribution):
- **simbuilder** = pipeline-app. Web UI MOVED from `Scenario Builder\App` → **`pipeline-app\web\`** (index/style/app.js/vehicle.js/motorbuilder.js + old ScenarioBuilder.exe launcher + launcher-src). Old App folder holds ONLY `build\Test Run` (George's run archives; his exe settings.json runs_dir points there — do not delete) + MOVED.md. `main.py web_index()` → BASE/web. **Build simplified: `--add-data "web;web"` (whole folder — ends forgotten-file bugs) + `--paths "..\CSV to MDF Converter\mf4-viewer-app"`** (viewer's new home) + mf4viewer.ico from there too; full command in README. Portfolio `.claude\launch.json` scenario-builder preview now serves pipeline-app\web. settings.json gitignored.
- **mf4-viewer** = `CSV to MDF Converter\mf4-viewer-app` (viewer.py + spec + ico EXTRACTED from csv-to-mf4-app; canonical for both standalone MF4Viewer.exe — now built in ITS dist — and SimBuilder import). Uses csv app's shared venv.
- **plt-to-mf4-converter** = plt-to-mf4-app (README added).
- **csv-to-mf4-converter** (existing) = csv-to-mf4-app, converter-only now (viewer removed, README pointers).

- Flow: builder ADF → `pipeline.py` makes self-contained run folder under `PhD Thesis\Simulation Runs\<name>_<timestamp>`, copies+patches deck (renames to scenario, resolves `../` refs against source deck dir, HEALS stale absolute paths e.g. `C:/Users/gfare/...` by copying same-named file from source deck folder), writes ADF under deck's referenced name, copies `.nam` (comes from MotionView export, NOT the solver run) → `motionsolve.bat <deck>` headless subprocess (no window ever appears; log streamed) → PLT→MF4 (modules copied verbatim from plt-to-mf4-app — keep in sync!) → launches MF4Viewer.exe with result.
- Progress bar parses solver `Time=` lines vs total sim time from [MANEUVERS_LIST] caps. JS bridge: Python evaluate_js → `msPipe.log/status/progress/done`.
- **ALL-IN-ONE exe** (George's requirement 2026-07-16): `dist\MotionSolvePipeline.exe` (~58 MB) contains builder+pipeline AND the MF4 viewer AND the PLT converter via self-dispatch (`--viewer [files]`, `--plt-converter`; Chrome multi-process pattern — pywebview & tkinter can't share a process loop). Viewer imported from csv-to-mf4-app\viewer.py via `--paths` (canonical, auto-fresh at build); plt GUI copied as `plt_gui.py`. Header buttons + auto-view spawn self. Bundles SNAPSHOT of builder web files — rebuild after builder edits (full command in README). `settings.json`: deck, motionsolve.bat, runs_dir, pack_voltage.
- STOP button kills the whole solver tree via psutil children(recursive) leaves-first (`pipeline.kill_process_tree`) — plain `taskkill /T` orphaned msolve/mbd_d/msdaemon/radflex. Progress bar parses solver `Time=` lines vs [MANEUVERS_LIST] total.
- **Vehicle fields made REAL 2026-07-16** per George's demand: `motor_gen.py` GENERATES the motor FMU .mat files from motor specs — schema reverse-engineered: `motor_char.mat` scalars `m_spd_max_f/r` [rad/s], `m_spd_rated(_rpm)_f/r`, `m_trq_rated_f/r`; `frnt/rear_motor_data.mat`: `m_spd_data`(1,15), `m_max_trq`(1,15) envelope=min(Tpeak,P/ω), `m_map_eff_spd/trq`, `m_eff_map`(15,14), regen variants (1,27)/(15,27). Eff map upload per motor card (.csv grid: row0=torque Nm, col0=speed rpm, cells eff 0-1 or %; or .mat with m_eff_map) re-gridded via bilinear interp; synthetic PMSM bowl map otherwise. Mass: `pipeline.patch_mass` adds delta to heaviest Body_Rigid (chassis 1000 kg, id 30301) + scales its inertia; solver confirmed "Total Mass: 2.400E+03". `opt_trq_ratio.mat` (VCU split map 151×198) deliberately untouched. Toggles in UI: generate_motors / apply_mass (default ON). Deck has NO aero force element → Cd/area stay 📋 (preview only). Validated: HotRod run (500 Nm motors, 2400 kg) vs stock — end speed 18.5→12.3 km/h, accel +60%. NOTE: gentle scenarios demand ~0 motor torque (no aero, flat road) — use aggressive scenarios to see motor differences. requires scipy (installed in shared venv).
- **Presets + MotorBuilder v1 2026-07-16**: Scenario tab has a preset card (top of left column, 8 chips: 10-70+100% pedal) loading George's tip-in framework (0→50 kph step to X%, coast to 20, X% for 5 s; caps 90/90/60, hmax 0.01; preserves preview object; confirm() before replacing). **MotorBuilder = 3rd tab** (`motorbuilder.js`, remember in exe --add-data!): 5 sliders (peak eff %, peak loc speed/torque %, spd/trq falloff) → live 15×14 canvas heatmap w/ per-cell % + hover readout; "Use for Motor 1/2" pins NORMALIZED map to `veh.motors[i].effMapInline={spd_frac,trq_frac,eff}` (motor card shows "MotorBuilder custom map"); CSV export in upload format w/ motor's physical units. motor_gen precedence: effMapInline > effMapPath > synthetic (inline scaled by motor's w_max/T_peak then regridded). Validated: peak lands within 0.01 of set value. switchTab generalized for 3 tabs.
- **h_max slider re-ranged 2026-07-16 per George**: 10 ms is the FLOOR ("never need finer"), slider 0.01→0.1 s log; readScenarioInputs clamps hmax ≥ 0.01; stored scenarios migrated up. Standstill warning added to #warnings when startSpeedKph < 5 (his Test_Run_1/02 crawled at H≈4e-5, 0.59 sim-s in 3.5 min — the "runs taking forever" complaint was standstill stiffness, NOT h_max). George's frozen exe keeps its own settings.json in `pipeline-app\dist\` — his custom runs_dir: `Scenario Builder\App\build\Test Run`.
- **Gear ratios made REAL + UX round 2026-07-16**: `pipeline.patch_gear_ratios` patches the per-axle `"Pinion to Carrier (final drive ratio)"` Constraint_Coupler coefficients (front id 364002, rear 365002; sign = rotation direction, preserved). VERIFIED: coupler coefficient IS the complete motor→wheel reduction (measured EM/wheel speed = 3.700 stock). Correct default gearRatio = 3.7 (was fake 10.5; localStorage migration `_v:2` resets stale 10.5→3.7). Tabs reordered: Vehicle Builder FIRST, default tab; switchTab redraws scenario canvases (hidden canvases don't paint). Greyed eff-upload bug fixed: pywebviewready fires after initial render → re-render motor cards on that event. Motor .mat rows removed from Wheels card (only tire override there); leftover mats (opt_trq_ratio VCU) under Powertrain "Advanced" details. Green chip next to RUN: "🚗 runs vehicle: <name> ⚡". Steering rack coupler (8.5, id 317001/321001) identified but NOT yet patchable via UI.
- **Vehicle serial number 2026-07-16** (George's provenance ask): `computeSerial()` in vehicle.js — FNV-1a 32-bit over stableStringify of veh minus {serial, notes, _v}, motors sliced to motorCount; format `SN-<10 decimal digits>`. Deterministic: same spec = same serial (verified incl. revert-returns-original; notes excluded). Shown in Garage stats + run chips; in payload spec → manifest line + `converter.convert(serial_number=int)` writes constant **VehicleSerial** channel (unit '-', comment has SN) into every MF4. NOTE: pipeline-app/converter.py now DIVERGES from plt-to-mf4-app copy (serial_number kwarg) — don't blind-recopy over it. `window.vehInfo()` accessor added (veh is top-level let, NOT window.veh — that guard was a latent bug, fixed).
- **Layout feedback 2026-07-16**: George asked for denser full-width layout (multi-column card flow, sticky header, maximized window), then REVERTED it same day — "I don't like this, put it back." Keep the centered 1500px, single-column-cards layout. If he complains about scrolling again, offer SMALL targeted changes (e.g., reorder cards, collapse sections), not a wholesale re-flow.
- **MotorBuilder tab EXISTS** (built in a parallel session 2026-07-16): `motorbuilder.js` in Scenario Builder\App — slider-shaped efficiency island, live canvas heatmap, hover readout, pin-to-motor (`veh.motors[i].effMapInline`, normalized fractions; precedence: inline > uploaded file > synthetic in motor_gen), CSV export. Third tab "motor"; switchTab handles all three + calls mbRefresh on entry. **CRITICAL: exe rebuilds MUST `--add-data` ALL FIVE web files incl. motorbuilder.js** — omitting it shipped a dead Motor Builder tab ("vis broke", fixed same day). Full command in pipeline-app README — always copy the README command, never retype from memory. Parallel sessions may add web files: check `ls Scenario Builder\App\*.js` before any rebuild.
- **EMS Builder 2026-07-16** (`ems_builder.py`): regenerates the dual-motor torque-split map `optimal_torque_ratio_map` (r_ch grid) per run. **VERIFIED the FMU consumes it at vcu_type=4** by empirical two-map test (r_ch=0→EM2 carries 0%, r_ch=0.5→50%; MUST use MODERATE throttle ~35% — full throttle saturates both motors at peak and masks the split). Map structure (from Motor_PMSM_dual.fmu modelDescription.xml + default .mat): `w`(1,151) motor speed 0..1571 rad/s, `T_dem`(1,198) 0..591 Nm, `r_ch`(151,198) = fraction to SECONDARY motor (0=single, 0.5=even); axes preserved, only r_ch regenerated. FMU inputs are ONLY (vehicle speed, throttle, motor speed f/r) — torque split is NOT a live input, so online per-step EMS via FMU impossible; map-injection (Route 1) is the way. FMU params also expose `vcu_type`(=4 in deck, =3 FMU default — built-in strategy selector, semantics unknown), regen_psi, traction_max/gamma, inverter/converter_efficiency=0.95, SOC_initial=0.75. Strategies: loss_optimal (instantaneous ECMS using motor eff maps — reproduces deck's single-motor-low/share-high shape), rule (threshold), fuzzy, even, single_motor, deck_default(off). UI: EMS card in Vehicle Builder with **Enable toggle (default OFF — George's explicit "toggle to turn on/off" ask)** + strategy dropdown. Payload `ems:{enabled,strategy,params}` → apply_ems in pipeline.apply_vehicle after motor gen. Rebuild adds `--hidden-import ems_builder --hidden-import motor_gen`. Compare strategies via EM1/EM2 torque+eff in MF4 viewer. This is the thesis EMS-comparison crux (fuzzy vs Hamiltonian/ECMS vs DP vs rule); tie-in: MotionSolve→MF4→AVL drivability = RL/optimization reward; opt_trq_ratio.mat = the action space.
- **Preview inheritance + provenance 2026-07-16**: Scenario tab preview model inherits mass/CdA/Fmax live from Vehicle Builder via `#pmInherit` checkbox (default ON; fields disabled while inherited; `vehPreviewParams()` in vehicle.js; vehRefresh triggers scenario refresh when inherited). Garage button now just re-enables inheritance. Run provenance: vehicle-name chip in run panel header + **vehicle manifest block logged at top of every run** (`apply_vehicle` in pipeline.py: motors w/ ratings+ratio, mass, tire, voltage, with "[NOT APPLIED - toggle off]" markers). Solver time-step slider (log 1e-4..1e-2 s → h_max, solve-time estimate at measured 14 wall-s/sim-s @1ms; `SOLVE_SPEED_AT_1MS` in app.js). **PARKED/NEXT: "MotorBuilder" third tab — interactively draw/build efficiency maps & lookup tables** (George asked 2026-07-16 "maybe later"). NOTE: browser caches app.js — cache-bust when verifying via preview (exe unaffected).
- **Renamed SimBuilder 2026-07-16** (exe `dist\SimBuilder.exe`, old MotionSolvePipeline.exe deleted). Two tabs: Scenario Builder + **Vehicle Builder** (`vehicle.js` in Scenario Builder\App). Honest badges: ⚡ affects-sim (tire .tir override → all deck .tir refs re-pointed via `pipeline.apply_vehicle`; per-.mat overrides copied into run dir under original basename; pack voltage) vs 📋 spec-sheet (suspension dropdowns, wheelbase/track, Cd/area, motor cards, rims/tire spec, notes) → `vehicle.json` written into every run folder. Garage panel: derived stats (tire dia from "205/60R15" spec, gearing top speed, wheel torque, P/W, CdA) + live top-view SVG; "Use in scenario preview" pushes mass/CdA/Fmax into the point-mass preview. Vehicles persist in localStorage + save/load .vehicle.json. Suspension/mass geometry CANNOT be changed by form fields — baked into MotionView deck (badges make this honest in-UI). Pipeline setup card shows deck path/FMUs/mats/tire + Change buttons (deck, runs dir). Api additions: pick_file(filter), pick_runs_dir, run_scenario(name, adf, vehicle).
- MotionSolve 2025 installed locally (`C:\Program Files\Altair\2025`), licensed, batch works. Doublelane deck (10.77 s) solves in ~2 min; 5 s scenario in ~71 s.
- **Standstill launches are numerically expensive** (stiction → H~2e-5 s, order 1; a 122 s standstill scenario was killed after crawling). Rolling-start scenarios (VX0 > 0, mm/s = kph×277.778) run at H=1e-3. Advise start speed > 0.
- Validated end-to-end 2026-07-16: hand-built 5 s rolling-start ADF (builder format) → solver 71 s → MF4 20 ch, physics correct (30 km/h start, regen decel on tip-out). Builder ADF format thereby CONFIRMED valid for MotionSolve (was previously unvalidated).
- George's model deck: `Test Run For PY Script\Model_Run_doublelane_0.xml` + its `.nam`. FMU refs climb 7 levels (`../../../../../../../Program Files/Altair/...`); .mat params were gfare-absolute.
